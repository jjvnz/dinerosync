name: dinerosync
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^31.2.4  # Versión estable más reciente
  intl: ^0.20.2  # Versión actualizada
  uuid: ^4.5.1   # Versión actualizada para compatibilidad
  provider: ^6.1.4
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  google_fonts: ^6.1.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1  # Versión actualizada
  build_runner: ^2.4.13   # Versión actualizada
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
