name: dinerosync
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.2 <4.0.0'  # Modificado para mayor compatibilidad

dependencies:
  syncfusion_flutter_charts: ^29.1.33  # Versión estable más reciente
  intl: ^0.20.2  # Versión actualizada
  uuid: ^4.5.1   # Versión actualizada para compatibilidad
  provider: ^6.1.4
  hive_flutter: ^1.1.0

dev_dependencies:
  hive_generator: ^2.0.1  # Versión actualizada
  build_runner: ^2.4.15   # Versión actualizada

flutter:
  uses-material-design: true